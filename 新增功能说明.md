# dots.tts、独立字典与关键词音频

## 启动

1. 启动 `E:\AITTS\dots.tts\start_api.bat`，等服务预热完成。
2. 启动本项目新版程序，设置 → 使用的 TTS 服务 → **dots.tts**。
3. API 地址填 `http://127.0.0.1:9881`；保留默认的“流式播放（低延迟）”。dots.tts 使用 9881，GPT-SoVITS 使用 9880，两者可分别启动。
4. 参考音色留空时，弹幕姬优先从 `E:\AITTS\dots.tts\apps\gradio\default_prompts` 按文件名排序取首个音频；当前为 `hongchang.wav`。要用另一个音色，填 `zhubo.wav`。同名 `.txt` 优先于目录里的 `prompt_text`（`文件名不含扩展名|参考文本`），手动填写参考文本优先于两者。目录没有音频时才沿用服务默认音色；显式填写旧 `myvoice` 中的文件名仍受支持。
5. 在“音频测试”输入正文试听。填写可选用户名后，会按当前弹幕模板测试两套字典。完成后配置房间号、开启普通弹幕并开始监听。

本适配器针对你本机 `E:\AITTS\serve_api.py` 的 `POST /tts` 与 `POST /tts/stream`，不是其他同名项目的 OpenAI `/v1/audio/speech` 协议。无须再借用 GPT-SoVITS 兼容接口。

## 弹幕姬中的语速与音量

设置 → dots.tts 设置新增两个滑块：语速 **0.5–2.0 倍**，音量 **0–2.0 倍**。默认均为 1.0，音量 0 为静音。流式与非流式都生效，只处理 dots.tts 生成的语音，不改变关键词插播文件。

语速使用本机 `E:\AITTS\ffmpeg\bin\ffmpeg.exe` 的 atempo 进行变速不变调，仍按音频块处理，不等待整句生成；变速会增加少量处理缓冲。路径变化时可在弹幕姬设置中修改 FFmpeg 路径。原速时音量调整不需要 FFmpeg。放大音量时超出 PCM16 范围的采样会限幅。

为了让 HTTP 服务接收弹幕姬选中的本地预设文件，`E:\AITTS\serve_api.py` 只增加了对 `default_prompts` 目录的许可，其他目录仍不能访问，服务默认音色和 Gradio 界面没有改变。**已运行的 dots.tts 服务需要重启一次，新的参考目录支持才会生效。**

## 字典

设置 → B 站设置：

| 配置 | 示例 | 生效范围 |
| --- | --- | --- |
| 用户名别名字典 `{user_name}` | `Alice` → `小爱` | 弹幕、醒目留言、礼物、舰长中的用户名 |
| 消息字典 `{message}` | `yyds` → `永远的神` | 弹幕、醒目留言正文及正文试听 |
| 关键词音频 | `咕咕嘎嘎` → `E:\AITTS\sounds\咕咕嘎嘎.wav` | 原始消息正文中的关键词 |

原来的 `BiliService.AliasDict` 保留，成为“用户名别名字典”；原本用于正文的条目需要移到消息字典。所有引擎统一执行字面替换，区分大小写，同一位置长词优先，替换结果不再重复匹配。无需重启。界面显示的弹幕保持原文。

例如原词 A→B、B→C，原文 A 最终读 B，不会连锁变成 C。字典中的 `.`、`*` 等按普通字符处理，不是正则表达式。

## 关键词音频混播

准备自己的音频文件，在“关键词音频”中填入关键词，用文件夹按钮选择文件，或填入**本机绝对文件路径**。支持 WAV、MP3、FLAC、OGG；建议使用无首尾长静音的 WAV。文件需要一直保留在该路径。

收到 `前半句咕咕嘎嘎后半句` 后，依次播放：

`前半句（当前 TTS 音色） → 指定音频文件 → 后半句（当前 TTS 音色）`

同一句可重复出现关键词，也可配置多个关键词。音频匹配先于正文词典，且只匹配原始正文；词典替换新产生的关键词不会触发文件播放。用户名和模板固定文案不触发音频。

每条消息独占一个播放队列位置，其他弹幕不会插入片段之间。文件缺失或损坏会报错，并跳过该条播报，不会悄悄丢掉关键词；之后的消息仍可播放。试听失败会显示错误提示，直播播报错误可查看应用日志。

## 延迟与流式播放

默认流式模式通过 `/tts/stream` 收取 PCM16 WAV 数据，解析首部后立即向输出设备写音频，不等待整句响应完毕。这里是**音频分块输出**，输入仍为一条完整文本，不是逐个文字发请求。

混播也支持流式：先播前段的流式 TTS，再播文件，再请求后段的流式 TTS。这样首声更早，但片段交界处可能有短暂停顿（尤其文件后面需要等待下一段首包）；不承诺无缝拼接。

关闭流式后，会先合成整句的所有文本片段，再把片段统一解码为 48 kHz 双声道 WAV 后播放。此模式首次出声要等全部合成完成；拼接处理本身通常不是主要耗时。

排队、冷启动、模型预热及生成速度会影响首声延迟。播放队列按消息先后顺序执行，积压时需等待前一条播完。中途流式请求失败会停止该条，不自动重播，避免已播部分重复。

服务端原有的 `myvoice\读音修正.txt` 仍会继续处理传入的 TTS 文本；如果客户端字典替换后仍读音异常，也应检查服务端读音修正。

## 开发与验证

源码启动：`uv run src/main.py`。测试：`uv run pytest -q`。打包：`uv run pyinstaller --noconfirm kinoko7danmaku.spec`。

测试使用隔离的临时配置目录，不修改现有登录信息。可用环境变量 `KINOKO_DATA_DIR` 指定独立配置目录。未设置时仍使用原来的 `~/.kinoko7danmaku/`。
