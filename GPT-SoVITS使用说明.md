# GPT-SoVITS 弹幕语音

1. 打开左上方「TTS 设置」，在 TTS 服务中选择 GPT-SoVITS。
2. 选择安装文件夹：`E:\bangdreamai\bangdreamai\GPT-SoVITS-v2pro-20250604`。
3. 在「角色模型」下拉框选择角色，两份权重自动配对。安装新模型后点击「刷新」。
4. 点击「启动 / 检查 API」。首次加载需要等待，稍后再次检查，显示就绪后即可使用。
5. 为角色选择参考音频，填写音频原文和参考语言，在音频测试页面试听。

参考音频、参考文本、参考语言和无参考文本开关按每一组模型分别保存，重启软件后仍保留。首次选择新角色时参考信息为空，切回已配置角色自动恢复。更换安装路径后被视为新模型路径。

API 默认地址为 `http://127.0.0.1:9880`，无需添加 `/tts`。这是基于 `api_v2.py` 的接口，和 WebUI 网页地址不同。启动按钮使用整合包的 `runtime/python.exe` 和弹幕软件附带的 `kinoko_sovits_api.py`，后台运行且不弹控制台，日志在安装文件夹的 `kinoko-api.log`。它使用独立的 `kinoko-tts-infer.yaml`，不覆盖整合包原来的推理配置或 `api_v2.py`。API 独立运行，关闭弹幕软件不会关闭 API；再次打开软件会检查现有服务。

扫描范围为安装目录内的 `GPT_weights*` 和 `SoVITS_weights*`，支持 v1、v2、v2Pro、v2ProPlus（V2PP）、v3、v4。先按同版本同名配对，再识别 `-e15`、`_e6_s2370_l32` 等训练后缀。配对不明确或缺文件时显示诊断，不任意选择另一角色。

本机当前扫描到 53 组：52 组 V2ProPlus、1 组 V4。已通过专用 API 实际生成 V2ProPlus 的 32 kHz WAV 和 V4 的 48 kHz WAV；其他版本有扫描和协议支持，本次未逐个进行模型推理。参考音频路径必须能由 API 所在电脑访问。

新版 API 常驻当前一组模型：同角色连续播报复用，真正切换角色才重新加载。服务端检查模型文件和实际加载状态，选角色与合成由同一个请求串行处理，不依赖客户端猜测。连接未升级的原版 API 时会回退到每次设置权重的兼容流程。升级后需重开新版弹幕软件，且「启动 / 检查 API」应显示「模型常驻」；如果显示「原版」，需停止旧 API 后再由按钮启动。


## 流式播放

默认开启「流式播放」，适用于本地 api_v2 接口。音频测试和弹幕播报均会在收到首段 PCM 后开始播放，不等待整个请求结束。原先选「不切」时，流式模式自动使用按标点切分；关闭流式后仍按原配置生成完整音频。旧 WebUI 连接保持完整音频播放。

这是分句/分段流式，不是逐字输出；很短的单句仍需要等待该句合成完成。不会自动提高语速。首次请求与换角色后的首段时间包含模型加载；连续同角色在常驻服务上省去重复加载。

旧版多句文本实测报告为 `build/sovits-validation/stream-report.json`。新版固定文本、种子和参数的对照见 `GPT-SoVITS性能验证.md`：本机 V4、16 步，同角色暖短句首段约 0.73～0.75 秒，多句首段 0.81 秒、完整返回 2.42 秒。此为本机采样结果，非固定延迟保证。测试覆盖提前产出 PCM、零长度 WAV 流头、分包、关闭流时释放连接，以及独立于 dots.tts 开关的路由。
