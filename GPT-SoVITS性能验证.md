# GPT-SoVITS 性能验证（2026-09-19）

定位到的主要延迟：旧适配器每条播报都调用两次设置权重接口。相同角色也会重新加载，V4 暖请求中仅加载两份权重就花费约 2.42～3.12 秒。用户名查找本身不是这个延迟的来源。

新版使用服务端常驻模型与原子合成接口，同一角色连续播报复用当前模型；真正换角色、模型文件改变、外部设置权重或加载失败时重新加载。只有一组模型常驻，频繁交替多个角色仍有加载成本。

## 本机前后对照

在独立端口启动整合包真实 API，使用当前诗歌剧 V4、16 步、1.0 倍速和相同参考音频、Top K/P/温度；固定随机种子 1234。两组均为流式 WAV、batch_size=1、cut5。测量首个真实 PCM，未把 WAV 文件头当作首音频；采集到文件，未通过扬声器播放。没有修改用户配置。

| 请求 | 旧流程首段 PCM | 新流程首段 PCM | 旧流程完整返回 | 新流程完整返回 |
| --- | ---: | ---: | ---: | ---: |
| 冷启动后首次短句 | 6.72 s | 5.85 s | 6.86 s | 6.00 s |
| 同角色短句，第 2 次 | 3.89 s | 0.73 s | 4.03 s | 0.86 s |
| 同角色三句文本 | 3.52 s | 0.81 s | 5.02 s | 2.42 s |
| 同角色短句，第 4 次 | 3.16 s | 0.75 s | 3.32 s | 0.88 s |

短句音频长度均为 2.56 秒，多句为 6.16 秒。新版四次请求加载模型 1 次、复用 3 次。多句首段早于完整音频约 1.61 秒，确认分段流式仍在运行。四组前后 WAV 的 SHA-256 分别完全相同（包括 PCM），见 `audio-equivalence.json`。结果基于本机少量样本，不能作为所有角色、文本、机器或 GPU 负载下的性能保证，也不等于完成主观音质评测。

原始结果：`build/sovits-performance/baseline-report.json`、`optimized-report.json`，同目录有服务日志、WAV 和复现实测脚本。真正切换角色的五条入队验证及报告在 `build/sovits-performance/role-validation/`。

## 如何区分等待来源

- 日志「排队」：前面的弹幕还没读完，等待随积压增长。
- 日志「准备请求」：接口检查，以及连接原版 API 时的权重设置。常驻接口把加载包含在后续首段时间内。
- 日志「首段音频」：开始处理该条到收到可播放 PCM，包括首次加载、实际换角色、合成及传输。
- 日志「音频流消费完成」：还包含下游播放等待，不是纯 GPU 推理时间。

分句流式不会逐字生成。一个短句仍要等该句的首段合成；上一条未播完时也不会提前换角色。弹幕长期到达速度超过朗读速度，串行队列仍会积压。
