# 用户字典与多 TTS 服务

左上方新增「用户字典」，原用户名发音字典、正文替换字典和关键词音频集中在这里。现有字典数据无需迁移。

在「用户字典」给每个用户名选择服务与音色：

1. 先在「TTS 设置」配置要用的服务。GPT-SoVITS 需要保存各模型的参考音频、文本和语言；Fish Audio 需要密钥，并在音色列表中添加音色；dots 使用服务已加载的固定模型。
2. 输入弹幕中的原始用户名，选择 dots.tts、GPT-SoVITS 或 Fish Audio，再选择对应角色或已保存音色，点击「保存 / 更新」。用户名精确匹配，区分大小写，匹配发生在用户名发音替换之前。
3. 总开关和该用户的独立开关都开启时，按绑定播报；未配置、已停用或总开关关闭时，使用全局默认服务。绑定不会修改「TTS 设置」中的默认服务和音色。

可以在本地服务未启动时提前保存绑定。旧 GPT-SoVITS 绑定和独立开关继续保留；现在默认服务无论是 dots、GPT-SoVITS 还是 Fish Audio，都能使用这些绑定，原 dots 专用跨服务开关不再限制它们。

## 本地检测与临时回退

「检测本地服务」可以手动刷新 dots 和 GPT-SoVITS 的状态。检测只请求状态或接口说明，不合成语音、不启动 API、不加载新模型。云端 Fish Audio 直接使用已配置的 API，不依赖本地服务。

轮到一条弹幕时，先检查其指定的本地服务，使用短超时和短期缓存。服务未就绪时，按用户选择临时使用入队时的默认服务，并在试听页或主页消息列表显示原因。默认服务也是本地部署时，同样检查它；两者都不可用则本条报错，继续后续队列。

回退只发生在合成前的可用性检查阶段。合成报错、鉴权失败或流式传输中断后，不自动换服务重读，避免重复播报或再次产生云端请求费用。探测结果不能保证随后的推理一定成功；服务随时可能停止。

dots 默认 `http://127.0.0.1:9881`，GPT-SoVITS 默认 `http://127.0.0.1:9880`。程序不会为每条弹幕启停服务。不同服务共用一个播放队列，一条读完才处理下一条；从 GPT 切到 Fish Audio 或 dots 不会主动卸载本地模型。真正换 GPT 角色、服务重启或其他客户端改变模型时仍有加载成本。

入队时保存指定服务与默认服务的参数快照，包括模型或音色、参考信息、API、流式开关和采样参数。之后修改设置、更新映射或切换默认角色，都不会改变已排队的消息。无参考配置或模型文件缺失时该条明确失败，不会借用其他角色的参考。

本版直接运行 `dist-dots-recovery/Kinoko7Danmaku.exe`。304 项自动化检查通过，21 项服务端检查因应用环境缺少依赖跳过，其中新增的 13 项 dots OOM 服务端检查已在 dots 环境单独通过。新增覆盖显存不足提示及服务状态；保留三种服务间路由、开关、旧绑定、音色保存与切换、状态探测缓存、离线回退、顺序播放、设置快照、失败后继续及流式断流不重试。

上一版真实本地 API 只读检查：GPT-SoVITS 正常响应（248 毫秒），dots 在 1 秒内未响应；超时不等同于确认进程已经停止。该轮没有启停服务或发起语音合成，多服务播放与回退使用隔离模拟测试。报告与界面截图在 `build/multi-tts-check/`。

## dots 显存不足处理

dots 报出 `CUDA out of memory` 时，问题发生在 GPU 推理阶段；流式客户端可能只看到连接提前结束。新版服务会记录显存不足，健康检查显示不可用，并阻止故障后反复推理。首段音频生成失败时直接返回明确错误；中途失败则中止流，弹幕软件再查询一次状态以显示原因，不重复合成。

当前 `E:/AITTS/dots.tts/start_api_2p.bat` 已启用 `DOTS_TTS_LOW_VRAM=1`，保留 Torch 编译，但关闭 CUDA Graph 工作区缓存。模型、精度和弹幕软件中设置的 32 步保持不变；此选项可能增加推理延迟，实际显存节省取决于请求和其他 GPU 程序。修改在重启 dots 服务后生效，`/health` 的 `low_vram: true` 可确认启用。软件保留用户原来的流式开关选择。

OOM 后应释放显存并重启 dots。用户绑定的本地服务在合成前检测为不可用时，按既定规则临时使用默认服务；已经开始合成的本条不自动重读。修复和测试证据保存在 `build/dots-stream-fix/`。

本轮实测：用户重启 dots 后，`low_vram` 为 `true`。保留 32 步和 1.2 倍速，通过实际 StreamPlayer 将一条短句按播放时长送往静音输出：首写 1.189 秒，输出音频 2.251 秒，包含播放等待的流程 4.261 秒，正常关闭且无 OOM。请求结束后 dots 自报预留显存 7.95 GiB；该值不是整张显卡占用，也不是所有文本的显存上限。本轮只发起一条真实合成，未做长时间负载测试或真实扬声器试听。新 EXE 已通过隔离配置的离屏启动检查，并校验 11 个打包模块与已测试源码一致。

## 切换成本

GPT 和 SoVITS 权重真正切换时需要加载模型，会产生等待。新版由「启动 / 检查 API」启动模型常驻服务，同角色连续播报复用当前模型及有效的参考特征，不再重复加载。用户名查找本身开销很小；A → A 会复用，A → B → A 仍要分别加载，只常驻一组模型。第一次请求仍有加载和预热时间。

模型状态由服务端检查，模型文件变化、外部切换或加载失败会使缓存失效。选模型与合成在同一个请求中完成，并锁定到流式响应结束，防止中途被其他请求切换角色。播放队列等上一条播放完成后才开始下一条的加载和合成，因此切换不会打断上一条。连接原版 API 时仍兼容旧流程，但没有这项加速。

## 队列语义与验证

本机接收回调进入播放队列的顺序为 FIFO。一个任务完整包含合成、文本分段、本地关键词音频及播放。只有一个消费者，流式片段也不会与下一条交叉。单条失败会记录错误并继续处理后续消息；手动停止会取消当前及待播放任务。礼物合并本来就会延迟，不能将所有不同事件类型的合并播报顺序理解成平台全局时间顺序。

队列无长度上限。如果长时间每秒进入的文本比每秒能读完的文本多，等待会持续增长；串行队列能避免同时推理/播放，并不意味着支持无限弹幕量或保证所有请求无错误。

此前版本的验证证据（不作为本版真实多服务推理测试）：

- 全部 59 项自动化测试通过。突发测试一次提交 24 条，分别覆盖完整音频和流式、多片段原子播放、FIFO、最多一个输出、中间失败后继续、取消后释放资源并重启。
- 独立本地 API 实测四条同时提交：爱音 → 诗歌剧 → 爱音 → 未配置用户（默认诗歌剧），使用真实 V2ProPlus/V4 模型生成并采集 PCM。权重与参考对应正确，默认配置未改变，最多一个输出。音频采集到文件，未通过扬声器播放。
- 实测脚本、报告、四份 WAV、隔离服务日志位于 `build/user-voices-validation/`。该结果是本地管线验证，不是实际连接直播间的多人弹幕压测或长期容量测试。

性能版本追加验证（2026-09-19）：应用测试 70 项通过，8 项 API 测试因应用环境没有 FastAPI 而跳过；这 8 项已在整合包 runtime 中补跑，其服务端测试共 16 项全部通过。新版真实 API 五条同时入队 A → A → B → A → 默认 B，覆盖 V2ProPlus/V4，结果为加载 4 次、复用 1 次、最多一个音频输出；参考配置、默认模型均正确。PCM 采集及报告位于 `build/sovits-performance/role-validation/`，仍不等同于扬声器试听或直播间容量压测。

性能对照详见 `GPT-SoVITS性能验证.md`。日志现在分别记录入队等待、请求准备、首段 PCM 时间；「音频流消费完成」包含播放等待，不能直接当作纯推理耗时。

混合路由追加验证：应用测试 87 项通过，8 项未改动的服务端 API 测试因应用环境没有 FastAPI 继续跳过。新增测试覆盖两种真实适配器的混合队列、流式/非流式组合、失败后继续、入队后更改全局设置、开关持久化及默认关闭。HTTP 使用模拟响应，不等同于真实双模型推理。

本轮真实双 API 验证未完成：请求前 dots API 已停止，随后按用户要求停止额外实测，临时测试实例已终止，未继续启动服务。过程记录保留在 `build/mixed-routing-validation/`。不将此前单引擎实测或模拟队列测试当作本轮双 API 实测结果。
