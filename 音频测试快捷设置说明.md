# 音频测试快捷设置

直接运行 `dist-user-services/Kinoko7Danmaku.exe`，保留原有用户配置。

音频测试输入框下方可以直接切换 TTS 服务。页面只显示该服务的试听参数，不显示 API 地址或密钥。

- GPT-SoVITS：下拉选择成对角色模型、调语速、推理步数、流式开关。切换角色时恢复该角色记住的参考音频和文本。
- dots.tts：沿用服务已加载的固定模型，直接调语速、推理步数和流式开关；步数 0 表示沿用服务默认设置。
- “更多参数”可展开参考音频/文本、Top K、Top P、采样温度、句间停顿等对应服务支持的选项。
- fish.audio：生成模型、已保存音色下拉列表、语速、流式播放；更多参数内可调延迟模式、音量、温度及 Top P。
- MiniMax、旧 Fish Speech、Piper、Edge 的入口已隐藏，旧配置保留。服务选择只显示 dots.tts、GPT-SoVITS、Fish Audio。

修改会同步保存到 TTS 设置，影响下一次试听和之后新加入队列的弹幕。已排队的 GPT-SoVITS / dots / Fish Audio 语音保留入队时的配置。

输入已配置角色映射的用户名时，试听仍按该用户名的映射发声，页面会提示实际使用的角色。清空用户名即可测试下方直接选择的服务和模型。

用户字典支持为各用户名选择 dots.tts、GPT-SoVITS 或 Fish Audio，保留总开关和每个用户的独立开关；停用只恢复默认服务和角色，不删除绑定。本地目标服务不可用时临时使用默认服务，详情见 `用户角色与弹幕队列说明.md`。

fish.audio 已按本机用户选择配置 S2.1 Pro Free 和指定音色，密钥仅保存在用户配置中，不打入 EXE。使用官方 REST PCM 流，逐段接收并播放音频；弹幕按整条文本提交。官方 WebSocket 另支持增量输入文字，但仍会缓冲合成，不保证每个字立即发声。

前一版验证：132 项自动化检查通过，8 项未安装 FastAPI 的服务器测试跳过。覆盖服务入口隐藏、配置同步、角色映射开关、参考记忆、试听配置传递、原有混合队列与流式解析；完成 Qt 离屏截图检查。指定的 Fish Audio 免费模型和音色实测一条短句成功，首段 PCM 1.844 秒、音频长度 1.811 秒（当时只接收并保存音频，没有经过按播放时长消费的播放器）。

## Fish Audio 音色列表

TTS 设置和音频测试共用同一份音色列表，修改后会立即同步。

1. 点击“切换音色”旁的“添加”，粘贴音色 ID 或 fish.audio 音色页面链接。
2. 在旁边填写名称，或点击“自动获取名称”。名称留空时，“保存并使用”会尝试查询官方名称；查询失败也可以手动命名后保存。
3. 以后直接下拉切换。“编辑”可以改名；相同 ID 更新名称，不会重复添加。
4. “从列表移除”只删除本机条目。移除正在使用的音色后，需要重新选择音色。

当前指定音色已查询为“莫提斯”。获取名称仅查询模型信息，不合成语音。

## 流式试听排查

用户反馈取消流式后可以播放。本版保留用户关闭流式的选择；整段和流式调用同一个 Fish Audio PCM 接口，区别在于何时交给播放器。

报错现在会区分连接失败、读取超时和传输中断，同时标明是否已经收到部分音频。不会自动重发请求，以免重复合成或重读。

使用 qasync 和实际 StreamPlayer 做过一条短句在线测试，以按真实音频时长消费的静音输出代替声卡：首写 1.492 秒，2.554 秒音频，4.161 秒完成并关闭，未复现此次网络失败。离线测试覆盖边接收边播放和中途断流释放资源。这些结果不等同于真实声卡试听或证明此前故障已经修复。

测试证据保存在 `build/fish-voices-check/`。

本版自动化验证：193 项通过，8 项因未安装 FastAPI 跳过；新增覆盖音色保存、名称查询、双页面同步、关闭弹窗时的异步处理、旧配置迁移和流式播放。
